There will be a difference on Verify for manual_line.key (bd027) in 
the format.  The key file specifies the format and the new output
agrees with it.  The old version in Stdout does not agree, which is
a bit worrisome.

28 Jan 2004.

